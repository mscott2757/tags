import styled from 'styled-components';

export const Input = styled.input`
  border: 1px solid #ddd;
  border-radius: 2px;
  &:focus {
    outline: none;
  }
  padding: 0 16px;
  height: 44px;
  line-height: 44px;
  font-size: 12px;
`;
